Use `postgres_run_query` when the task requires direct SQL against the user's connected Postgres database. It always uses the saved integration credentials; never ask for or place connection details in a tool call. Prefer parameterized queries with `binds`. Leave `write` unset for reads, and set it only when the user explicitly asks to change database state. Never expose the connection string, password, or database-provider spend.
